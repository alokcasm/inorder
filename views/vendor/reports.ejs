<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Reports - inOrder</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #07090F; color: #ffffff; overflow-x: hidden; }
        .bg-gradient-primary { background-image: linear-gradient(135deg, #FF6B00, #FF3D3D); }
    </style>
</head>
<body class="antialiased selection:bg-orange-500/30">

    <!-- ========================================= -->
    <!-- RESPONSIVE NAVBAR                         -->
    <!-- ========================================= -->
    <nav class="bg-[#0B0E16]/90 backdrop-blur-xl border-b border-white/[0.05] sticky top-0 z-40">
        <div class="max-w-7xl mx-auto px-5 sm:px-6 py-4 flex justify-between items-center">
            
            <!-- Logo -->
            <div class="flex items-center gap-3">
                <div class="w-8 h-8 rounded-lg bg-gradient-primary flex items-center justify-center shadow-[0_0_15px_rgba(255,107,0,0.4)]">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
                </div>
                <span class="text-xl font-bold tracking-tight"><%= user.shopName %></span>
            </div>
            
            <!-- Desktop Links (Hidden on Mobile) -->
            <div class="hidden lg:flex items-center gap-6">
                <a href="/vendor/dashboard" class="text-gray-400 hover:text-white font-medium transition">Live Orders</a>
                <a href="/vendor/menu" class="text-gray-400 hover:text-white font-medium transition">Menu & QR</a>
                <a href="/vendor/reports" class="text-orange-500 font-medium">Reports</a>
                <a href="/vendor/settings" class="text-gray-400 hover:text-white font-medium transition">Settings</a>
                <a href="/logout" class="bg-white/10 hover:bg-white/20 px-4 py-2 rounded-xl text-sm font-bold transition">Logout</a>
            </div>

            <!-- Mobile Hamburger Button -->
            <button onclick="openSidebar()" class="lg:hidden text-gray-300 hover:text-white p-1 focus:outline-none">
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="3" y1="12" x2="21" y2="12"></line><line x1="3" y1="6" x2="21" y2="6"></line><line x1="3" y1="18" x2="21" y2="18"></line></svg>
            </button>
        </div>
    </nav>

    <!-- ========================================= -->
    <!-- MOBILE SIDEBAR (Off-canvas)               -->
    <!-- ========================================= -->
    
    <!-- Dark Overlay Backdrop -->
    <div id="sidebarOverlay" onclick="closeSidebar()" class="fixed inset-0 bg-black/60 backdrop-blur-sm z-50 hidden opacity-0 transition-opacity duration-300"></div>

    <!-- Sliding Sidebar Menu -->
    <div id="mobileSidebar" class="fixed top-0 right-0 w-64 h-full bg-[#0B0E16] border-l border-white/5 z-[60] transform translate-x-full transition-transform duration-300 ease-in-out flex flex-col shadow-2xl">
        
        <!-- Sidebar Header -->
        <div class="px-6 py-5 border-b border-white/5 flex justify-between items-center bg-[#121624]/50">
            <span class="font-extrabold text-white tracking-widest uppercase text-sm">Menu</span>
            <button onclick="closeSidebar()" class="text-gray-400 hover:text-white p-1 bg-white/5 rounded-full">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>
            </button>
        </div>

        <!-- Sidebar Links -->
        <div class="flex flex-col p-4 space-y-2 flex-1">
            <a href="/vendor/dashboard" class="flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-white hover:bg-white/5 font-semibold transition-colors">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect><line x1="3" y1="9" x2="21" y2="9"></line><line x1="9" y1="21" x2="9" y2="9"></line></svg>
                Live Orders
            </a>
            <a href="/vendor/menu" class="flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-white hover:bg-white/5 font-semibold transition-colors">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path><polyline points="14 2 14 8 20 8"></polyline><line x1="16" y1="13" x2="8" y2="13"></line><line x1="16" y1="17" x2="8" y2="17"></line><polyline points="10 9 9 9 8 9"></polyline></svg>
                Menu & QR
            </a>
            
            <!-- Active State for Reports -->
            <a href="/vendor/reports" class="flex items-center gap-3 px-4 py-3 rounded-xl bg-orange-500/10 text-orange-500 border border-orange-500/20 font-bold transition-colors">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="20" x2="18" y2="10"></line><line x1="12" y1="20" x2="12" y2="4"></line><line x1="6" y1="20" x2="6" y2="14"></line></svg>
                Reports
            </a>

            <a href="/vendor/settings" class="flex items-center gap-3 px-4 py-3 rounded-xl text-gray-400 hover:text-white hover:bg-white/5 font-semibold transition-colors">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"></circle><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"></path></svg>
                Settings
            </a>
        </div>

        <!-- Sidebar Footer -->
        <div class="p-5 border-t border-white/5">
            <a href="/logout" class="flex justify-center items-center gap-2 w-full bg-red-500/10 hover:bg-red-500/20 text-red-500 font-bold py-3.5 rounded-xl border border-red-500/20 transition-colors">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path><polyline points="16 17 21 12 16 7"></polyline><line x1="21" y1="12" x2="9" y2="12"></line></svg>
                Sign Out
            </a>
        </div>
    </div>


    <main class="max-w-7xl mx-auto px-5 sm:px-6 py-8">
        <h1 class="text-3xl font-bold mb-8">Sales & Analytics</h1>

        <!-- Stat Cards -->
         <div class="grid grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6 mb-10">
            <!-- Today -->
            <div class="bg-[#121624] border border-white/5 rounded-2xl p-5 shadow-lg border-t-2 border-t-orange-500">
                <p class="text-gray-400 text-xs sm:text-sm font-medium mb-1">Today's Revenue</p>
                <h2 class="text-2xl sm:text-3xl font-bold text-white">₹<%= todaysRevenue %></h2>
                <p class="text-xs text-gray-500 mt-2"><%= todaysOrdersCount %> orders today</p>
            </div>
            <!-- Last 30 Days -->
            <div class="bg-[#121624] border border-white/5 rounded-2xl p-5 shadow-lg border-t-2 border-t-blue-500">
                <p class="text-gray-400 text-xs sm:text-sm font-medium mb-1">Last 30 Days</p>
                <h2 class="text-2xl sm:text-3xl font-bold text-white">₹<%= thirtyDaysRevenue %></h2>
                <p class="text-xs text-gray-500 mt-2"><%= thirtyDaysOrdersCount %> orders this month</p>
            </div>
            <!-- All Time -->
            <div class="bg-[#121624] border border-white/5 rounded-2xl p-5 shadow-lg border-t-2 border-t-emerald-500 col-span-2 lg:col-span-1">
                <p class="text-gray-400 text-xs sm:text-sm font-medium mb-1">All-Time Revenue</p>
                <h2 class="text-2xl sm:text-3xl font-bold text-white">₹<%= totalRevenue %></h2>
                <p class="text-xs text-gray-500 mt-2"><%= totalOrders %> lifetime orders</p>
            </div>
        </div>

        <!-- Order History & Ratings -->
        <h2 class="text-xl font-bold mb-4">Recent Completed Orders</h2>
        <div class="bg-[#121624] border border-white/5 rounded-2xl overflow-hidden shadow-lg">
            <div class="overflow-x-auto">
                <table class="w-full text-left border-collapse min-w-[600px]">
                    <thead>
                        <tr class="bg-[#0B0E16] text-gray-400 text-sm border-b border-white/5">
                            <th class="p-4 font-medium">Order ID</th>
                            <th class="p-4 font-medium">Date & Time</th>
                            <th class="p-4 font-medium">Table</th>
                            <th class="p-4 font-medium">Amount</th>
                            <th class="p-4 font-medium">Rating & Feedback</th>
                        </tr>
                    </thead>
                    <tbody class="text-sm">
                        <% if(recentOrders.length === 0) { %>
                            <tr><td colspan="5" class="p-8 text-center text-gray-500">No completed orders yet.</td></tr>
                        <% } else { %>
                            <% recentOrders.forEach(order => { %>
                                <tr class="border-b border-white/5 last:border-0 hover:bg-white/[0.02] transition-colors">
                                    <td class="p-4 font-mono text-xs text-gray-400">#<%= order._id.toString().slice(-6) %></td>
                                    <td class="p-4 text-gray-300"><%= new Date(order.createdAt).toLocaleString() %></td>
                                    <td class="p-4 font-bold text-white"><%= order.tableNumber %></td>
                                    <td class="p-4 text-emerald-400 font-bold">₹<%= order.totalAmount %></td>
                                    <td class="p-4">
                                        <% if(order.rating) { %>
                                            <span class="text-yellow-400"><%= '⭐'.repeat(order.rating) %></span>
                                            <% if(order.comment) { %>
                                                <p class="text-xs text-gray-400 italic mt-1">"<%= order.comment %>"</p>
                                            <% } %>
                                        <% } else { %>
                                            <span class="text-gray-600 text-xs">No rating</span>
                                        <% } %>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </main>

    <!-- Script for Sidebar Toggle -->
    <script>
        const sidebar = document.getElementById('mobileSidebar');
        const overlay = document.getElementById('sidebarOverlay');

        function openSidebar() {
            overlay.classList.remove('hidden');
            setTimeout(() => overlay.classList.remove('opacity-0'), 10);
            sidebar.classList.remove('translate-x-full');
            document.body.style.overflow = 'hidden'; // Stop background scrolling
        }

        function closeSidebar() {
            sidebar.classList.add('translate-x-full');
            overlay.classList.add('opacity-0');
            setTimeout(() => overlay.classList.add('hidden'), 300);
            document.body.style.overflow = '';
        }
    </script>
</body>
</html>